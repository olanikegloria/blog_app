<div class="container">
  <div class="row justify-content-center align-items-center" style="min-height: 100vh;">
    <div class="col-md-6">
      <h1 class="text-center mb-4">Add Commenet to post</h1>

      <%= form_with model: @comment, url: user_post_comments_path, local: true do |form| %>
        <div class="form-group">
          <%= form.label :text %>
          <%= form.text_area :text, class: "form-control", placeholder: "Add your comment" %>
        </div>
        <%= form.submit "Add Commment", class: "btn btn-primary btn-block" %>
      <% end %>
    </div>
  </div>
</div>
